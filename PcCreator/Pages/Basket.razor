@inherits LayoutComponentBase
@inject IBasketService BasketService
@inject NavigationManager UriHelper
@inject IPCitemService PCItemService
@page "/basket"
@using System.ComponentModel;  


<table class="table table-dark">
  <thead>
    <tr>
      <th scope="col">Name</th>
      <th scope="col">Memory</th>
      <th scope="col">Cores</th>
      <th scope="col">Vendor</th>
      <th scope="col">Type of Component</th>
      <th scope="col">Speed</th>
      <th scope="col">Virtual Memory</th>
    </tr>
  </thead>
  @foreach(var p in BasketService.basket)
    {
         <thead>
             <tr>
             
             <th scope="col">@p.Key @p.Value</th>
             </tr>
              </thead>    
    }
   
            }
            }
            </table>





 

@code {

    protected override void OnInitialized()
    {
        BasketService.LoadBasketItems();
        PCItemService.LoadPcItems();
        foreach (var i in BasketService.basket)
        {
            if (PCItemService.pclist.PCItem. == i)
            {

            }
        }

    }


    

  
   
   
}
